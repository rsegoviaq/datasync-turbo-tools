# DataSync Turbo Tools - Basic Configuration
# Minimal configuration for quick evaluation and single-file uploads

# ============================================================================
# AWS SETTINGS
# ============================================================================

# AWS Profile (optional - uses default if not set)
export AWS_PROFILE="datasync-test"

# AWS Region
export AWS_REGION="us-east-1"

# ============================================================================
# S3 SETTINGS
# ============================================================================

# S3 Bucket Name (REQUIRED - change this!)
export S3_BUCKET="datasync-filmflow-20251022"

# S3 Subdirectory (optional - leave empty for root)
export S3_SUBDIRECTORY="uploads"

# Storage Class
export S3_STORAGE_CLASS="STANDARD"

# ============================================================================
# SOURCE SETTINGS
# ============================================================================

# Source directory to upload (REQUIRED - change this!)
export SOURCE_DIR="/home/ray_segovia/FilmFlow-datasync-poc/deployment/source"

# ============================================================================
# S5CMD PERFORMANCE SETTINGS
# ============================================================================

# Number of concurrent operations (recommended: 32-64)
export S5CMD_CONCURRENCY="32"

# Multipart upload chunk size (recommended: 64MB-256MB)
export S5CMD_PART_SIZE="64MB"

# Number of worker goroutines (recommended: 16-32)
export S5CMD_NUM_WORKERS="16"

# Retry count for failed uploads
export S5CMD_RETRY_COUNT="3"

# Log level (debug|info|warn|error)
export S5CMD_LOG_LEVEL="info"

# Checksum algorithm (CRC64NVME|SHA256)
# CRC64NVME is faster and hardware-accelerated
export S5CMD_CHECKSUM_ALGORITHM="CRC64NVME"

# ============================================================================
# LOGGING
# ============================================================================

# Enable/disable logging to file
export ENABLE_LOGGING="true"

# Log directory
export LOG_DIR="./logs"

# ============================================================================
# NOTES
# ============================================================================
#
# This is a minimal configuration for quick evaluation.
# For production deployments, see examples/production/
#
# Quick Start:
# 1. Edit BUCKET_NAME and SOURCE_DIR above
# 2. Run: ./upload.sh
#
# For more configuration options, see config/s5cmd.env.template
