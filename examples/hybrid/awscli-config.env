# DataSync Turbo Tools - AWS CLI Configuration
# Configuration for AWS CLI reference implementation

# ============================================================================
# AWS SETTINGS
# ============================================================================

export AWS_PROFILE="default"
export AWS_REGION="us-east-1"

# ============================================================================
# S3 SETTINGS
# ============================================================================

export BUCKET_NAME="my-test-bucket"
export S3_SUBDIRECTORY="awscli-uploads"
export S3_STORAGE_CLASS="STANDARD"

# ============================================================================
# SOURCE SETTINGS
# ============================================================================

export SOURCE_DIR="/data/test/uploads"

# ============================================================================
# AWS CLI PERFORMANCE SETTINGS
# ============================================================================

# Max concurrent requests (AWS CLI default: 10, recommended: 20)
export AWS_MAX_CONCURRENT_REQUESTS="20"

# Multipart threshold (AWS CLI default: 8MB, recommended: 64MB)
export AWS_MULTIPART_THRESHOLD="64MB"

# Multipart chunk size (AWS CLI default: 8MB, recommended: 64MB)
export AWS_MULTIPART_CHUNKSIZE="64MB"

# Max bandwidth (optional, e.g., "50MB/s")
export AWS_MAX_BANDWIDTH=""

# ============================================================================
# LOGGING
# ============================================================================

export ENABLE_LOGGING="true"
export LOG_DIR="./logs/awscli"
export ENABLE_JSON_OUTPUT="true"
export JSON_OUTPUT_DIR="./logs/awscli/json"

# ============================================================================
# NOTES
# ============================================================================
#
# This configuration uses AWS CLI for comparison with s5cmd.
# AWS CLI is slower but more mature and widely supported.
#
# Expected performance:
# - Network: 3 Gbps
# - Throughput: ~200-320 MB/s
# - Improvement over default: 2x
#
